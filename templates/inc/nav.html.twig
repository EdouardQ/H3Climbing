<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarToggler">
        <a class="navbar-brand" href="{{ url('homepage') }}"><img src="{{ asset('img/climbing-icon.png') }}"></a>
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item">
                <a class="nav-link" href="{{ url('discovery_day.list') }}">All Discovery Days</a>
            </li>
        </ul>
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0 navbar-user">
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item">
                    <img src="{{ asset('img/' ~ app.user.rank.name ~ '.png') }}" alt="" class="img-rank">
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('user.homepage') }}">My Space</a>
                </li>
                {% if app.user.rank.name == 'gold' %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('user.discovery_day.list') }}">Manage my Discovery Days</a>
                </li>
                {% endif %}
                <li class="navbar-blank">&nbsp;</li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('security.logout') }}">Log out</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('registration.register') }}">Sign In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('security.login') }}">Log In</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>